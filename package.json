{
  "name": "binlist-client",
  "version": "1.0.0",
  "description": "Client app to use bin list api and save responses",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hernan.pintos@edreamsodigeo.com",
  "license": "ISC",
  "dependencies": {
    "binlookup": "^2.0.1",
    "objects-to-csv": "^1.3.6"
  }
}
